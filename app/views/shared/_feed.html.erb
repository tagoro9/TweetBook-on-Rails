<% if !@feed_items.empty? %>
    <%= render :partial => 'shared/feed_item', :collection => @feed_items, :renderer => BootstrapPagination::Rails %>
  <%= will_paginate @feed_items %>
<% else%>
	<p>Comienza a seguir a gente para que tu Timeline se llene</p>
<% end %>