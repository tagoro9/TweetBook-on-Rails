<!DOCTYPE html>
<html>
	<head>
  		<title><%= @title %></title>
		<%= csrf_meta_tag %>
		<%= render 'layouts/stylesheets' %>
	</head>
	<body>
		<div class="container" id="content">
			<%= header %>
			<%= yield %>
			<%= footer %>
			<%= debug(params) if Rails.env.development? %>
		</div>		
	</body>
</html>
