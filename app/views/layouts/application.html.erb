<!DOCTYPE html>
<html>
	<head>
  		<title><%= @title %></title>
		<%= csrf_meta_tag %>
		<%= render 'layouts/stylesheets' %>
	</head>
	<body>
		<%= header %>
		<%= yield %>
		<%= footer %>
		<%= debug(params) if Rails.env.development? %>		
	</body>
</html>
